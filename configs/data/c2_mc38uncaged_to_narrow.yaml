# @package _global_

defaults:
  - default@data

data:
  path: "/mnt/direct-attached/GENERALIZATION_DATA/TRAINING_DATA_C2_MC38UNCAGED_TO_NARROW"
  val_name: 'val'
  test_name: 'test'
  train_name: 'train'

initialization:
  load_from_checkpoint: "/mnt/personal/cellanome/checkpoints/RFDETR-Seg-ckpts/output/checkpoint_best_ema.pth"

checkpointing:
  save_dir: "/home/ubuntu/scratch/cellanome/checkpoints/"
  rtdetr_initial_checkpoint: "/home/ubuntu/scratch/cellanome/checkpoints/${checkpointing._folder_name}"
  dinov2_backbone_checkpoint: "/mnt/personal/cellanome/checkpoints/backbones/dinov2"

hydra:
  run:
    dir: "/home/ubuntu/scratch/cellanome/logs/${now:%Y-%m-%d}/${now:%H-%M-%S}_C2_MC38UNCAGED_TO_NARROW"
