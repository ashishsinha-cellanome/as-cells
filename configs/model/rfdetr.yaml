name: rf_detr_${model.rfdetr.size}
input_size: 560

rfdetr:
  size: base             # base|small|medium|large|nano
  pretrain_weights: null # auto-select size-matched defaults unless explicitly overridden
  num_queries: null
  num_select: null

  # RF-DETR data/training behavior
  multi_scale: true
  expanded_scales: true
  do_random_resize_via_padding: false
  square_resize_div_64: true
  use_ema: true
  ema_decay: 0.993
  ema_tau: 100
  lr_scheduler: cosine
  warmup_epochs: 1.0
  lr_min_factor: 0.0

# --- General ---
label_map:
  0: "cell"
  1: "bead"
  2: "cell-adhered"
  3: "soma"

detection_threshold: 0.05
draw_threshold: 0.4
max_detections: 100

ema:
  enabled: true
  decay: 0.993
  tau: 100
  warmup_steps: 100
